import TimerContentPage from "./components/timer-content";

export default function PomodoroPage() {
  return (
    <main className="relative w-full flex flex-col items-center justify-center">
      <TimerContentPage />
    </main>
  );
}